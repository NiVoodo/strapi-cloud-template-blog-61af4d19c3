{
  "collectionName": "components_blocks_hero_banners",
  "info": { "displayName": "Hero Banner", "description": "Large visual header with copy & CTAs" },
  "options": {},
  "attributes": {
    "eyebrow": { "type": "string" },
    "title": { "type": "string", "required": true },
    "richText": { "type": "richtext" },
    "media": {
      "type": "media",
      "multiple": false,
      "allowedTypes": ["images"]
    },
    "overlay": { "type": "boolean", "default": false },
    "align": {
      "type": "enumeration",
      "enum": ["left", "center", "right"],
      "default": "left"
    },
    "buttons": {
      "type": "component",
      "repeatable": true,
      "component": "shared.button"
    }
  }
}
